///cria_texto(x,y,str,cor_out,cor,nome);
    var cria = instance_create(argument0,argument1,obj_texto);
    cria.str = argument2;
    cria.cor_out = argument3;
    cria.cor = argument4;
    cria.nome = argument5
    return cria;
